name: StaffUtils
version: '${project.version}'
main: dev.poetty.staffutils.StaffUtils

commands:
  tiempo:
    description: Muestra el tiempo total de juego de un usuario.
    aliases: [t]
  reset:
    description: Resetea la informacion de un jugador.
    aliases: [ r ]
